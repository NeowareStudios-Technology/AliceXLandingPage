/*
	LWA alice one landing page script
*/
$(window).on('scroll', function() {
	// $parrot = $('#parrotModel');
	// $pCont = $('.parrotContainer');
	// var offset = $pCont.offset();
	// $pCont.html( "left: " + offset.left + ", top: " + offset.top );
	// $parrot.show();
	// 	if( $(this).scrollTop() == $parrot.position().top ){
	//       $parrot.animate({
	//     		opacity: 0.25,
	//     		left: "+=50",
	// 		    height: "toggle"
	// 		  }, 5000, function() {
	// 		    // Animation complete
	//
	// 		  });
	// 	}
})
